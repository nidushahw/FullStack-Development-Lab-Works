function greaterThanSize() {
    let size = Math.random() * 10;
    let counter = 0;
    while (counter < size) {
        counter++;

        console.log("The counter size is " + size + " and count is " + counter);
    }
}
greaterThanSize(); 